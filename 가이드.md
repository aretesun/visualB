# 📘 Visual Board 프로젝트 가이드

## 📋 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [폴더 구조](#폴더-구조)
4. [아키텍처 설명](#아키텍처-설명)
5. [주요 컴포넌트](#주요-컴포넌트)
6. [상태 관리](#상태-관리)
7. [개발 가이드](#개발-가이드)

---

## 프로젝트 개요

**Visual Board**는 드래그 앤 드롭으로 비전보드를 만들 수 있는 인터랙티브 웹 애플리케이션입니다.

### 주요 기능
- ✏️ 텍스트 카드 생성 및 편집
- 🖼️ 이미지 추가 (업로드, URL, AI 생성)
- 🎯 드래그 앤 드롭으로 자유로운 배치
- 🔄 카드 크기 조절 및 이미지 위치 조정
- 🎨 스티커 기능
- 🌐 다국어 지원 (한국어, English)
- 💾 자동 저장 (localStorage)
- 🔗 공유 기능

---

## 기술 스택

```json
{
  "프레임워크": "React 19.2.0",
  "빌드툴": "Vite 6.2.0",
  "언어": "TypeScript 5.8.2",
  "스타일링": "Tailwind CSS v4",
  "상태관리": "Zustand 5.0.8",
  "배포": "GitHub Pages"
}
```

---

## 폴더 구조

```
visualB/
├── components/          # 리액트 컴포넌트
│   ├── Card/           # 카드 컴포넌트 (분해된 구조)
│   │   ├── Card.tsx              # 메인 카드 컨테이너
│   │   ├── CardText.tsx          # 텍스트 편집
│   │   ├── CardImage.tsx         # 이미지 표시/편집
│   │   ├── CardControls.tsx      # 컨트롤 버튼
│   │   └── index.ts              # Export
│   ├── AddCardButton.tsx         # 카드 추가 버튼
│   ├── ImageSourceDropdown.tsx   # 이미지 소스 선택
│   ├── ImageUrlModal.tsx         # URL 입력 모달
│   ├── LinksMenu.tsx             # 링크 메뉴
│   ├── SettingsMenu.tsx          # 설정 메뉴
│   ├── ShareModal.tsx            # 공유 모달
│   ├── StickerObject.tsx         # 스티커 객체
│   ├── StickerPalette.tsx        # 스티커 팔레트
│   ├── Toast.tsx                 # 토스트 알림
│   ├── Toolbar.tsx               # 상단 툴바
│   └── VisionItem.tsx            # [Deprecated] 기존 카드 컴포넌트
│
├── contexts/           # React Context
│   └── LanguageContext.tsx       # 다국어 지원
│
├── hooks/              # Custom Hooks
│   ├── useCards.ts               # 카드 관련 로직
│   ├── useDraggable.ts           # 드래그 기능
│   ├── useImageEditor.ts         # 이미지 편집
│   └── useStickers.ts            # 스티커 관련 로직
│
├── services/           # 비즈니스 로직 (순수 함수)
│   ├── cardService.ts            # 카드 CRUD 로직
│   ├── stickerService.ts         # 스티커 관리
│   ├── shareService.ts           # API 통신
│   └── storageService.ts         # localStorage 래퍼
│
├── store/              # 상태 관리 (Zustand)
│   └── useStore.ts               # 전역 상태 스토어
│
├── utils/              # 유틸리티 함수
│   ├── constants.ts              # 상수 정의
│   ├── imageUtils.ts             # 이미지 처리
│   └── positionUtils.ts          # 위치 계산
│
├── locales/            # 다국어 리소스
│   ├── ko.ts                     # 한국어
│   ├── en.ts                     # 영어
│   └── index.ts                  # Export
│
├── sticker/            # 스티커 이미지
│   ├── santa.png
│   └── tree.png
│
├── App.tsx             # 메인 앱 컴포넌트
├── types.ts            # TypeScript 타입 정의
├── index.tsx           # 엔트리 포인트
└── vite.config.ts      # Vite 설정
```

---

## 아키텍처 설명

### 계층 구조

```
┌─────────────────────────────────────────┐
│         Components (UI Layer)           │  ← 사용자 인터페이스
├─────────────────────────────────────────┤
│         Hooks (Logic Integration)       │  ← Store + Service 연결
├─────────────────────────────────────────┤
│      Store (State Management)           │  ← 전역 상태 (Zustand)
├─────────────────────────────────────────┤
│      Services (Business Logic)          │  ← 순수 함수, 테스트 가능
├─────────────────────────────────────────┤
│         Utils (Utilities)               │  ← 공통 유틸리티
└─────────────────────────────────────────┘
```

### 설계 원칙

1. **관심사 분리 (Separation of Concerns)**
   - UI 로직 → Components
   - 상태 관리 → Store
   - 비즈니스 로직 → Services

2. **단일 책임 원칙 (Single Responsibility)**
   - 각 컴포넌트는 하나의 명확한 목적
   - 큰 컴포넌트는 작은 단위로 분해

3. **재사용성 (Reusability)**
   - Custom Hooks로 로직 재사용
   - Services는 순수 함수로 구현

---

## 주요 컴포넌트

### 1. App.tsx (790 lines)
**역할**: 메인 애플리케이션 컨테이너

**주요 기능**:
- 전역 상태 관리 (Zustand Store 연동)
- 카드 및 스티커 렌더링
- 이벤트 핸들러 통합
- 다중 선택 (Ctrl+Click)
- 선택 박스 드래그

**주요 로직**:
```typescript
// Store 연결
const cards = useCanvasStore(state => state.cards);
const addCard = useCanvasStore(state => state.addCard);

// 카드 위치 업데이트 (다중 선택 지원)
const updateItemPosition = (id, position, delta) => {
  if (delta && selectedCards.has(id)) {
    // 선택된 모든 카드를 함께 이동
    moveMultipleCards(selectedCards, delta);
  } else {
    updateCard(id, { position });
  }
};
```

---

### 2. components/Card/ (분해된 카드 컴포넌트)

#### 2.1 Card.tsx (567 lines)
**역할**: 카드 메인 컨테이너

**책임**:
- 드래그 앤 드롭 기능 (`useDraggable` hook)
- 리사이즈 핸들 관리 (8방향)
- 이미지 압축 및 업로드
- 드롭다운 메뉴 관리
- 자식 컴포넌트 통합

**Props**:
```typescript
interface CardProps {
  item: Card;                    // 카드 데이터
  onPositionChange: (id, pos, delta?) => void;
  onTextChange: (id, text) => void;
  onImageChange: (id, url) => void;
  onImageSizeChange: (id, w, h) => void;
  onImageOffsetChange: (id, offset) => void;
  onDelete: (id) => void;
  onBringToFront: (id) => void;
  isSelected?: boolean;          // 선택 상태
  isReadOnly?: boolean;          // 읽기 전용 모드
}
```

#### 2.2 CardText.tsx (107 lines)
**역할**: 텍스트 편집 기능

**책임**:
- 텍스트 입력/편집 모드 전환
- 키보드 단축키 (Enter: 저장, Esc: 취소)
- 자동 포커스
- 최대 길이 제한 (300자)

**특징**:
```typescript
// Enter로 저장
if (e.key === 'Enter' && !e.shiftKey) {
  handleSave();
}

// Esc로 취소
if (e.key === 'Escape') {
  setEditText(text || '');
  onEditEnd();
}
```

#### 2.3 CardImage.tsx (305 lines)
**역할**: 이미지 표시 및 위치 조정

**책임**:
- 이미지 표시 (`object-fit: cover`)
- 이미지 오프셋 자동 보정
- 드래그로 이미지 위치 조정
- 경계 감지 및 경고
- "이미지 추가" 버튼 (빈 카드)

**핵심 로직**:
```typescript
// 이미지 오프셋 보정 (카드 크기 변경 시)
useEffect(() => {
  correctImageOffset();
}, [imageWidth, imageHeight]);

// 드래그로 이미지 위치 조정 (잠금 해제 시)
const handleImageDragStart = (e) => {
  if (isImageLocked) return;
  // X, Y축 독립적으로 이동 가능 여부 체크
  // 경계 넘으면 경고 표시
};
```

#### 2.4 CardControls.tsx (155 lines)
**역할**: 컨트롤 버튼 모음

**버튼 목록**:
- 🔒 이미지 잠금/해제
- ✏️ 텍스트 편집
- ✅ 저장
- 🖼️ 이미지 편집
- 🗑️ 삭제

**조건부 렌더링**:
```typescript
{hasImage && <LockButton />}
{hasText && !isEditingText && <EditButton />}
{isEditingText && <SaveButton />}
{hasImage && !isEditingImage && <EditImageButton />}
<DeleteButton />
```

---

### 3. components/ (기타 UI 컴포넌트)

#### Toolbar.tsx
- 상단 툴바 (타이틀, 언어 선택, 공유 버튼 등)

#### AddCardButton.tsx
- 카드 추가 FAB (Floating Action Button)

#### StickerPalette.tsx
- 스티커 선택 팔레트

#### Toast.tsx
- 알림 메시지 (성공, 에러, 정보)

---

## 상태 관리

### Zustand Store (store/useStore.ts)

#### 1. CanvasStore (캔버스 상태)
```typescript
{
  cards: Card[],                  // 카드 목록
  viewport: Size,                 // 뷰포트 크기
  backgroundImage: string | null, // 배경 이미지

  // Actions
  addCard: (card?) => void,
  updateCard: (id, updates) => void,
  deleteCard: (id) => void,
  setCards: (cards) => void,
  bringCardToFront: (id) => void,
  // ...
}
```

#### 2. StickerStore (스티커 상태)
```typescript
{
  palette: Sticker[],             // 스티커 목록
  instances: StickerInstance[],   // 배치된 스티커들
  isPaletteExpanded: boolean,     // 팔레트 열림/닫힘

  // Actions
  addSticker: (sticker) => void,
  addInstance: (instance) => void,
  updateInstance: (id, updates) => void,
  deleteInstance: (id) => void,
  // ...
}
```

#### 3. SelectionStore (선택 상태)
```typescript
{
  selectedCards: Set<number>,     // 선택된 카드들
  selectedStickers: Set<number>,  // 선택된 스티커들

  // Actions
  selectCard: (id, isMulti) => void,
  deselectAll: () => void,
  // ...
}
```

#### 4. UIStore (UI 상태)
```typescript
{
  showToast: boolean,
  toastMessage: string,
  toastType: 'success' | 'error' | 'info',
  showShareModal: boolean,
  showSettingsMenu: boolean,
  // ...
}
```

---

## Custom Hooks

### hooks/useCards.ts
**역할**: 카드 관련 로직 통합

```typescript
export const useCards = () => {
  const cards = useCanvasStore(state => state.cards);
  const addCardToStore = useCanvasStore(state => state.addCard);

  const addCard = useCallback(() => {
    // CardService로 검증
    if (!CardService.canAdd(cards.length)) {
      return { success: false, error: 'MAX_CARDS_REACHED' };
    }

    // Store 업데이트
    addCardToStore({ position: centerPosition });
    return { success: true };
  }, [cards.length]);

  return { cards, addCard, updateCard, deleteCard };
};
```

### hooks/useDraggable.ts
**역할**: 드래그 앤 드롭 기능

**특징**:
- Pointer Events 사용 (터치 지원)
- 실시간 위치 업데이트
- 드래그 종료 시 최종 위치 저장

### hooks/useStickers.ts
**역할**: 스티커 관련 로직 통합

### hooks/useImageEditor.ts
**역할**: 이미지 편집 상태 관리

---

## Services (비즈니스 로직)

### services/cardService.ts
**역할**: 카드 관련 순수 함수

```typescript
export class CardService {
  // 카드 생성
  static create(position: Position, id: number): Card {
    return {
      id,
      position: PositionUtils.clamp(position, viewport),
      text: '',
      imageUrl: '',
      // ...
    };
  }

  // 추가 가능 여부 체크
  static canAdd(currentCount: number): boolean {
    return currentCount < CONSTANTS.MAX_CARDS;
  }

  // 여러 카드 동시 이동
  static moveMultiple(cards, selectedIds, delta): Card[] {
    // ...
  }
}
```

### services/stickerService.ts
**역할**: 스티커 관련 로직

### services/shareService.ts
**역할**: API 통신

```typescript
export class ShareService {
  static async save(items: Card[]): Promise<ShareResponse> {
    const response = await fetch(`${WORKER_URL}/save`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ items }),
    });
    return response.json();
  }

  static async load(id: string): Promise<Card[]> {
    // ...
  }
}
```

### services/storageService.ts
**역할**: localStorage 래퍼

```typescript
export class StorageService {
  static save(key: string, data: any): void {
    localStorage.setItem(key, JSON.stringify(data));
  }

  static load<T>(key: string): T | null {
    const data = localStorage.getItem(key);
    return data ? JSON.parse(data) : null;
  }

  static getUsage(): { used: number; total: number } {
    // localStorage 사용량 계산
  }
}
```

---

## Utils (유틸리티)

### utils/constants.ts
**모든 상수 중앙 관리**

```typescript
export const CONSTANTS = {
  MAX_CARDS: 50,
  IMAGE_QUALITY: 0.7,
  IMAGE_MAX_WIDTH: 800,
  WORKER_URL: 'https://api.example.com',
  STORAGE_KEYS: {
    CARDS: 'vision-board-cards',
    STICKERS: 'vision-board-stickers',
  },
  // ...
};
```

### utils/imageUtils.ts
**이미지 처리 함수**

```typescript
export class ImageUtils {
  // 이미지 압축
  static async compress(file: File, maxWidth = 800): Promise<string> {
    // Canvas로 리사이즈 및 품질 조정
  }

  // 이미지 크기 감지
  static async getSize(url: string): Promise<Size> {
    // Image 로드 후 크기 반환
  }

  // URL 유효성 검증
  static isValidUrl(url: string): boolean {
    // URL 패턴 체크
  }
}
```

### utils/positionUtils.ts
**위치 계산 함수**

```typescript
export class PositionUtils {
  // 위치 제한 (경계 내)
  static clamp(pos: Position, viewport: Size, size: Size): Position {
    return {
      x: Math.max(0, Math.min(viewport.width - size.width, pos.x)),
      y: Math.max(0, Math.min(viewport.height - size.height, pos.y)),
    };
  }

  // 겹침 감지
  static isOverlapping(rect1, rect2): boolean {
    // 사각형 충돌 감지
  }

  // 중앙 위치 계산
  static getCenterPosition(viewport: Size, size: Size): Position {
    // ...
  }
}
```

---

## 타입 정의 (types.ts)

```typescript
// 카드
export interface Card {
  id: number;
  position: Position;
  text?: string;
  imageUrl?: string;
  imageWidth?: number;
  imageHeight?: number;
  imageOffset?: Position;
}

// 위치
export interface Position {
  x: number;
  y: number;
}

// 크기
export interface Size {
  width: number;
  height: number;
}

// 스티커
export interface Sticker {
  id: number;
  imageUrl: string;
  name: string;
}

export interface StickerInstance {
  id: number;
  stickerId: number;
  position: Position;
  size: Size;
  rotation: number;
}
```

---

## 개발 가이드

### 시작하기

```bash
# 의존성 설치
npm install

# 개발 서버 실행 (http://localhost:5173)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview

# GitHub Pages 배포
npm run deploy
```

---

### 새로운 기능 추가하기

#### 1. 새로운 카드 기능 추가

**예시: 카드 복사 기능**

1. **Service에 로직 추가** (`services/cardService.ts`)
```typescript
static duplicate(card: Card, newId: number): Card {
  return {
    ...card,
    id: newId,
    position: {
      x: card.position.x + 20,
      y: card.position.y + 20,
    },
  };
}
```

2. **Store에 액션 추가** (`store/useStore.ts`)
```typescript
duplicateCard: (id: number) => {
  const card = get().cards.find(c => c.id === id);
  if (!card) return;

  const newCard = CardService.duplicate(card, Date.now());
  set(state => ({ cards: [...state.cards, newCard] }));
}
```

3. **Hook에서 사용** (`hooks/useCards.ts`)
```typescript
const duplicateCard = useCanvasStore(state => state.duplicateCard);
return { ..., duplicateCard };
```

4. **컴포넌트에서 호출** (`components/Card/CardControls.tsx`)
```tsx
<button onClick={() => onDuplicate()}>
  복사
</button>
```

---

#### 2. 새로운 유틸리티 함수 추가

**예시: 색상 관련 유틸**

1. **새 파일 생성** (`utils/colorUtils.ts`)
```typescript
export class ColorUtils {
  static hexToRgb(hex: string): RGB {
    // ...
  }

  static rgbToHex(rgb: RGB): string {
    // ...
  }
}
```

2. **constants.ts에 색상 상수 추가**
```typescript
export const COLORS = {
  PRIMARY: '#3B82F6',
  SUCCESS: '#10B981',
  ERROR: '#EF4444',
};
```

---

#### 3. 새로운 언어 추가

**예시: 일본어 추가**

1. **언어 파일 생성** (`locales/ja.ts`)
```typescript
export const ja = {
  card: {
    placeholder: 'テキストを入力...',
    addImage: '画像を追加',
  },
  // ...
};
```

2. **index.ts에 등록** (`locales/index.ts`)
```typescript
import { ja } from './ja';

export const translations = {
  ko,
  en,
  ja,  // 추가
};
```

3. **LanguageContext에 추가** (`contexts/LanguageContext.tsx`)
```typescript
type Language = 'ko' | 'en' | 'ja';
```

---

### 성능 최적화 체크리스트

#### ✅ 이미 적용된 최적화
- [x] React.memo로 컴포넌트 메모이제이션
- [x] Custom comparison 함수로 불필요한 리렌더링 방지
- [x] 이미지 압축 (800px, 70% 품질)
- [x] Zustand persist 미들웨어로 자동 저장
- [x] requestAnimationFrame으로 리사이즈 최적화

#### 🔄 향후 고려사항
- [ ] Virtual scrolling (카드가 많을 때)
- [ ] Web Worker로 이미지 압축 오프로드
- [ ] Lazy loading (코드 스플리팅)
- [ ] Service Worker (오프라인 지원)

---

### 디버깅 팁

#### 1. Zustand DevTools 사용
```typescript
// store/useStore.ts에서 이미 활성화됨
import { devtools } from 'zustand/middleware';

export const useCanvasStore = create<CanvasState>()(
  devtools(
    persist(...),
    { name: 'CanvasStore' }
  )
);
```

브라우저 Redux DevTools 확장으로 상태 변화 추적 가능.

#### 2. Console Logging
```typescript
// 컴포넌트 리렌더링 추적
useEffect(() => {
  console.log('Card re-rendered:', item.id);
});
```

#### 3. React DevTools Profiler
- 컴포넌트 렌더링 시간 측정
- 불필요한 리렌더링 식별

---

### 코딩 컨벤션

#### 파일 명명
- 컴포넌트: PascalCase (예: `CardText.tsx`)
- Hooks: camelCase with 'use' prefix (예: `useCards.ts`)
- Services: PascalCase with 'Service' suffix (예: `CardService`)
- Utils: PascalCase with 'Utils' suffix (예: `ImageUtils`)

#### 함수 명명
- Event Handlers: `handle` prefix (예: `handleClick`)
- Boolean returns: `is`, `has`, `can` prefix (예: `isValid`, `canAdd`)
- Actions: 동사 (예: `addCard`, `deleteCard`)

#### 주석
```typescript
// ❌ 나쁜 예
// x를 1 증가시킴
x++;

// ✅ 좋은 예
/**
 * 카드를 중앙에 배치합니다.
 *
 * @param viewport - 뷰포트 크기
 * @param cardSize - 카드 크기
 * @returns 중앙 위치 좌표
 */
function getCenterPosition(viewport: Size, cardSize: Size): Position {
  // ...
}
```

---

### 테스트 (향후 추가 예정)

#### Unit Tests (Services)
```typescript
// services/__tests__/cardService.test.ts
describe('CardService', () => {
  test('카드 생성', () => {
    const card = CardService.create({ x: 0, y: 0 }, 1);
    expect(card.id).toBe(1);
  });

  test('최대 개수 체크', () => {
    expect(CardService.canAdd(49)).toBe(true);
    expect(CardService.canAdd(50)).toBe(false);
  });
});
```

#### Integration Tests (Hooks)
```typescript
// hooks/__tests__/useCards.test.ts
import { renderHook, act } from '@testing-library/react';

test('카드 추가', () => {
  const { result } = renderHook(() => useCards());

  act(() => {
    result.current.addCard();
  });

  expect(result.current.cards.length).toBe(1);
});
```

---

## 트러블슈팅

### 자주 발생하는 이슈

#### 1. 이미지 오프셋이 이상해요
**원인**: 카드 크기 변경 시 이미지 오프셋 보정 로직 미작동

**해결**:
- `CardImage.tsx`의 `useEffect` 의존성 배열 확인
- `imgElementRef.current.complete` 체크 확인

#### 2. 드래그가 안 돼요
**원인**: 이벤트 전파 차단 또는 `disabled` 설정

**해결**:
- `useDraggable` hook의 `disabled` prop 확인
- `stopPropagation()` 호출 위치 확인

#### 3. localStorage 용량 초과
**원인**: 이미지를 base64로 저장하여 5MB 한계 도달

**해결**:
```typescript
// storageService.ts
const usage = StorageService.getUsage();
if (usage.used > 4 * 1024 * 1024) { // 4MB
  alert('저장 공간이 부족합니다.');
}
```

#### 4. 다중 선택이 안 돼요
**원인**: Ctrl/Cmd 키 감지 실패

**해결**:
```typescript
const isCtrlPressed = e.ctrlKey || e.metaKey; // Mac은 metaKey
```

---

## 참고 자료

### 공식 문서
- [React 19 Docs](https://react.dev/)
- [Zustand GitHub](https://github.com/pmndrs/zustand)
- [Tailwind CSS v4](https://tailwindcss.com/)
- [Vite Guide](https://vitejs.dev/guide/)

### 프로젝트 관련
- [README.md](./README.md) - 프로젝트 소개
- [이렇게.txt](./이렇게.txt) - 개발 노트
- [참고.JPG](./참고.JPG) - 디자인 참고

---

## 기여하기

1. 브랜치 생성: `git checkout -b feature/amazing-feature`
2. 변경사항 커밋: `git commit -m 'feat: Add amazing feature'`
3. 브랜치 푸시: `git push origin feature/amazing-feature`
4. Pull Request 생성

### Commit Convention
```
feat: 새로운 기능 추가
fix: 버그 수정
refactor: 코드 리팩토링
docs: 문서 수정
style: 코드 포맷팅
test: 테스트 추가
chore: 빌드 설정 등
```

---

## 라이선스

이 프로젝트는 MIT 라이선스 하에 있습니다.

---

## 문의

프로젝트 관련 문의사항은 Issues에 등록해주세요.

**마지막 업데이트**: 2025-11-13
**버전**: 0.0.0
